# GitHub Copilot Instructions

For this Playwright framework, whenever test cases are added or updated under `src/tests`, run:

```powershell
npm run validate
```

The validation command runs TypeScript type checking and ESLint. Fix all failures before completing the change.
