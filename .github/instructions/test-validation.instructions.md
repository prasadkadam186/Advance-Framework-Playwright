---
applyTo: "src/tests/**/*.ts"
---

When adding or modifying Playwright test cases, run `npm run validate` before considering the change complete.

Fix all TypeScript and ESLint errors before handing off the change.
