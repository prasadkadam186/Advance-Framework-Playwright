/**
 * index.ts
 *
 * Public barrel for the fixture layer.
 * Import `test` and `expect` from here in all spec files so the import
 * path stays stable regardless of future fixture restructuring.
 *
 * Usage:
 *   import { test, expect } from '@fixture/test-base';
 */

export { test, expect } from './test-base';
